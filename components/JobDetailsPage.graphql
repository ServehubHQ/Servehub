fragment JobDetailsPageJob on jobs {
  id
  target_name
  case_number
  lawyer_user_id

  server {
    id
  }

  attempts {
    id
    success
  }
}

fragment JobDetailsPageQuery on query_root {
  ...PageQuery
}
