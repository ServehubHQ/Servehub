query SettingsPage {
  current_user {
    id
    email_notifications_enabled
    notifications_enabled
    firebase_messaging_token

    address {
      id
      street
      unit
      postalCode: postal_code
      city
      province
    }
  }
  ...PageQuery
}
