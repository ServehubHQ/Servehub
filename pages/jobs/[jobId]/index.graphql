query JobDetials($jobId: uuid!, $userId: uuid) {
  jobs_by_pk(id: $jobId) {
    id
    description
    stripe_payment_intent_succeeded

    target_name
    target_address {
      id
      street
      unit
      postal_code
      city
      province
    }

    server {
      id
      name
    }

    attempts {
      id
      attempted_at
      success
    }

    ...JobDetailsPageJob
  }

  ...JobDetailsPageQuery
}
