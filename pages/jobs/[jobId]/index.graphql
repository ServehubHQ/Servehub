query JobDetials($jobId: uuid!, $userId: uuid) {
  users(where: { id: { _eq: $userId } }) {
    id
    ...PageUser
  }
  jobs_by_pk(id: $jobId) {
    id
    description
    stripe_payment_intent_succeeded

    target {
      id
      name
      ...JobMapTarget
    }

    server {
      id
    }

    attempts {
      id
      attempted_at
      success
    }

    ...ChatJob
  }
}
