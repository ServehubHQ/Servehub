query JobsCreatePlan($jobId: uuid!) {
  jobs(where: { id: { _eq: $jobId } }) {
    stripe_payment_intent_client_secret
    id
  }

  plans {
    id
    name
    fee
    bounty
    attempts
    duration
  }

  ...PageQuery
}
