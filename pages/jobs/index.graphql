query JobsList($userId: uuid) {
  users(where: { id: { _eq: $userId } }) {
    id
    ...PageUser
  }
  jobs {
    id
    description
    stripe_payment_intent_succeeded

    target {
      id
      name

      ...JobMapTarget
    }
  }
}
