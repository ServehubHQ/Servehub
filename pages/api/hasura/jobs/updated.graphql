query JobUpdated {
  users(
    where: {
      role: { role: { _eq: "server" } }
      firebase_messaging_token: { _is_null: false }
      approved: { _eq: true }
    }
  ) {
    firebase_messaging_token
    id
  }
}
